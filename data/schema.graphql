type Mutation {
  createNote(title: String!, prolog: String!, article: String!): Note
  # deleteNote(_id: ID): ID
  # updateNote(_id: ID, content: String): Note
}

type Note {
  _id: ID
  title: String
  prolog: String
  article: String
}

type Query {
  notes: [Note]
}
